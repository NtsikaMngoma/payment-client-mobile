import { CardPaymentRequest } from './card-payment-request';

describe('CardPaymentRequest', () => {
  it('should create an instance', () => {
    expect(new CardPaymentRequest()).toBeTruthy();
  });
});
